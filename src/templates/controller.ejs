export default (services) => ({
    handler: async({ body, query, params, headers }) => {
        try {
            return {
                status: 200,
                data:await services.service('app::<%= props.n||props.name %>.<%= props.f||props.file %>').execute({ m: 'dddd' })
            }
        } catch (error) {
            return {
                status: 500,
                data: { n: 'dd' }
            }
        }

    }
})