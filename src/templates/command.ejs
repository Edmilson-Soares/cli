export default ({ services, forawait }) => ({
    async execute(data,outputs){
 
        console.log(data,outputs)
    
        },
       async undo(data,outputs){
    
        console.log(data,outputs)
        }
})